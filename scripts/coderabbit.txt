Summary of suggestions:
  1. Unknown file: Starting CodeRabbit review in plain text mode...
  2. .claude/settings.json: Line: 30 to 31
  3. .claude/settings.json around lines 30-31, remove the redundant, path-specific entry ("Bash(git -C /Users/macbookuser/Code/mkcubed/next-js-01 status)") and keep the single generic "Bash(git status)" entry; ensure the file contains only the universal git status entry (no absolute paths) so the specific variant is deleted for security and simplicity.: ============================================================================
  4. .claude/settings.json: Line: 31
  5. .claude/settings.json around line 31, remove the redundant, user-specific entry "Bash(git -C /Users/macbookuser/Code/mkcubed/next-js-01 status)" because it hardcodes an absolute path and duplicates the generic "git status" permission on the previous line; either delete this line entirely or replace it with a relative/path-agnostic command (or keep only the generic "git status") so the configuration is portable and does not expose local directory structure.: ============================================================================
  6. packages/testing/tsconfig.json [nitpick]: Line: 5
  7. packages/testing/tsconfig.json around line 5, rootDir is set to "." which is too broad for a published package; change rootDir to "src" so compilation only includes source files, ensure your TypeScript sources are under packages/testing/src (or move them there), update any path references/imports or build scripts that assume the old root, and add/verify exclude/include entries to prevent config or non-source files from being emitted.: ============================================================================
  8. apps/routing/src/components/Button.test.tsx [nitpick]: Line: 38
  9. apps/routing/src/components/Button.test.tsx around line 38, the test uses a regex matcher getByRole("button", { name: ============================================================================
  10. packages/testing/tsconfig.json: Line: 8
  11. packages/testing/tsconfig.json around line 8, the include array currently lists config files (vitest.config.ts, vitest.setup.ts) which should not be compiled by TypeScript; remove those config files from the "include" array and keep only source files (e.g., "src//*") and any necessary declaration files (e.g., "vitest.d.ts"); if you still want type-checking for the config files during development, handle them via your IDE or a separate type-check script rather than the production tsconfig.: ============================================================================
  12. .github/workflows/pr.yml [nitpick]: Line: 175 to 190
  13. apps/routing/src/lib/example.test.ts [nitpick]: Line: 95 to 117
  14. .github/workflows/pr.yml: Line: 60 to 109
  15. .github/actions/turbo-filter/action.yml [nitpick]: Line: 21
  16. packages/testing/src/factories/index.ts [nitpick]: Line: 31 to 32
  17. packages/testing/src/factories/index.ts around lines 31-32, the direct re-export of faker couples consumers to the @faker-js/faker implementation; either remove the export or replace it with a controlled API: ============================================================================
  18. packages/testing/vitest.setup.ts [nitpick]: Line: 15 to 17
  19. packages/testing/vitest.setup.ts around lines 15 to 17, the afterEach callback currently wraps cleanup in an arrow function; simplify by passing cleanup directly to afterEach (i.e., replace afterEach(() => { cleanup(); }) with afterEach(cleanup)) to make the code more concise while preserving behavior.: ============================================================================
  20. .github/workflows/pr.yml [nitpick]: Line: 38 to 44
  21. .github/workflows/pr.yml around lines 38 to 44 the manual workflow input full_run currently defaults to true which makes full CI the default for manual dispatches; change the default to false so filtered runs are the default, and optionally update the description to indicate that setting full_run=true will force a full CI run when explicitly requested.: ============================================================================
  22. packages/testing/package.json: Line: 26 to 30
  23. packages/testing/package.json around lines 26 to 30, @testing-library/dom is missing as an explicit dependency (it is a peer of @testing-library/react) which can cause version/hoisting issues in monorepos; add "@testing-library/dom": ============================================================================
  24. packages/config/package.json: Line: 13 to 15
  25. packages/config/package.json around lines 13-15, the dependency on "@repo/testing" creates a circular-risk with that package (which likely imports config for Vitest); remove the direct dependency here and instead either (A) move any shared Vitest config into a new lightweight package (e.g., @repo/vitest-config) that both @repo/config and @repo/testing can depend on, or (B) change @repo/testing to depend only on base Vitest and export helpers without importing @repo/config, or (C) refactor @repo/config so it no longer imports from @repo/testing; implement one of these approaches and update package.json and import paths accordingly, then run the monorepo build/test to verify no circular dependency remains.: ============================================================================
  26. packages/config/package.json [nitpick]: Line: 7 to 12
  27. packages/config/package.json around lines 7 to 12, the package exports four vitest modules but the review indicates the corresponding source files may be missing; verify that src/vitest/base.ts, src/vitest/coverage.ts, src/vitest/setup-react.ts, and src/vitest/setup-msw.ts are present in the commit and correctly compiled by TypeScript (correct file names, paths, and included in tsconfig/includes). If any are missing, add the files to the repo or remove/update the export entries to point to existing files; if present but not built, ensure tsconfig and build scripts include src/vitest and that the files are checked into the commit.: ============================================================================
  28. packages/testing/package.json [nitpick]: Line: 7 to 18
  29. packages/testing/package.json around lines 7 to 18, the exports map references multiple source files (e.g., src/index.ts, src/render.tsx, src/providers.tsx, src/types.ts, src/mocks/index.ts, src/mocks/server.ts, src/mocks/handlers.ts, src/factories/index.ts, src/factories/user.ts, src/factories/organization.ts) that must be present in this commit; verify each referenced file exists at the exact path and filename (including .ts/.tsx extensions and correct casing) in the repo, and if any are missing either add the corresponding source file(s) to this commit or remove/adjust the export entry to point to an existing file so the package export map is accurate.: ============================================================================
  30. packages/testing/package.json [nitpick]: Line: 42 to 46
  31. packages/testing/package.json around lines 42 to 46, the peerDependencies allow React/react-dom >=18.0.0 with no upper bound which could permit React 20+; update the react and react-dom peerDependency ranges to include an upper bound (for example "<20.0.0") while leaving vitest unchanged so consumers cannot accidentally install untested major React versions; modify the package.json peerDependencies entries for "react" and "react-dom" to the tightened range and save the file. [major]: ============================================================================
  32. package.json [nitpick]: Line: 35
  33. .github/actions/setup/action.yml [nitpick]: Line: 29 to 38
  34. .github/actions/setup/action.yml around lines 29 to 38, the workflow silently continues if .nvmrc is missing or pnpm setup fails and lacks any verification or pinned version details; update the setup steps to fail fast on errors (ensure continue-on-error is not set or explicitly false), validate .nvmrc exists before invoking setup (or provide a documented fallback Node version), pin the actions to specific maintained tags or SHAs if needed (rather than an ambiguous tag), and add a follow-up "Verify setup" step that prints Node and pnpm versions so CI logs clearly show what was installed. [error]: ============================================================================
  35. packages/testing/src/providers.test.tsx [nitpick]: Line: 65 to 73
  36. packages/testing/src/providers.test.tsx around lines 65 to 73, the "renders without options" test duplicates the earlier "renders children correctly" test; remove this redundant test or change it to cover a distinct edge case (for example [error]: ============================================================================
  37. packages/testing/src/index.ts [nitpick]: Line: 57 to 61
  38. packages/testing/src/index.ts around lines 57 to 61, the file currently re-exports everything from "./mocks" and "./factories" using wildcard exports which can expose unintended API surface and cause naming conflicts; update this to explicit named exports by importing and then re-exporting only the intended symbols from each module (e.g., list and export server, setupMswServer, specific mock helpers, and each factory you want public), or if you must keep grouped exports, first audit both modules to ensure no name collisions and then explicitly export a consolidated, documented list of allowed symbols to maintain a stable, clear public API.: ============================================================================
  39. scripts/coderabbit-local-review.ts [nitpick]: Line: 78 to 149
  40. scripts/coderabbit-local-review.ts around lines 78 to 149, the regex/heuristic parsing is fragile and may break if CLI output changes; update the file by adding a concise top-of-function comment documenting the heuristics and their fragility, then implement detection for structured JSON output (try JSON.parse on trimmed output or look for a JSON marker) and parse that into Suggestion[] when available, falling back to the existing heuristic logic only if no JSON is present; keep existing fallbacks and truncation behavior intact and log or surface a clear note/comment recommending requesting JSON from the CodeRabbit CLI for robustness. [suggestion]: ============================================================================
  41. packages/config/vitest/coverage.ts: Line: 101 to 103
  42. packages/config/vitest/coverage.ts around lines 101 to 103, the comment for the skipFull option is misleading — it currently says it skips files with no tests, but skipFull actually controls whether files with 100% coverage are excluded from reports; update the comment to state that skipFull excludes files with full (100%) coverage when true, and that setting it to false includes those files in the report.: ============================================================================
  43. packages/testing/src/render.tsx: Line: 61 to 64
  44. packages/testing/src/render.tsx around lines 61 to 64, the current spread of renderOptions allows a user-provided wrapper to override the internal Wrapper, breaking the custom rerender logic; fix by ensuring the internal Wrapper takes precedence (e.g. spread renderOptions first and then set wrapper: ============================================================================
  45. packages/testing/src/render.test.tsx [nitpick]: Line: 20 to 93
  46. packages/testing/src/render.test.tsx around lines 20 to 93, the suite only covers happy paths; add edge-case tests to improve robustness by [error]: ============================================================================
  47. scripts/coderabbit-local-review.ts: Line: 55
  48. scripts/coderabbit-local-review.ts around line 55, the shell command string contains a double space between "--prompt-only" and "--base"; remove the extra space so the flags are separated by a single space: ============================================================================
  49. scripts/coderabbit-local-review.ts: Line: 189
  50. scripts/coderabbit-local-review.ts around line 189, the main function is marked async but never uses await which is unnecessary and can cause unhandled promise rejections when invoked without awaiting (line 248); remove the async modifier from the function declaration (and any implicit promise-returning behavior) so main is a plain synchronous function, and ensure any internal asynchronous operations are either awaited where needed or handled with .catch to avoid unhandled rejections when called without await.: ============================================================================
  51. packages/testing/src/providers.test.tsx [nitpick]: Line: 20 to 119
  52. packages/testing/src/providers.test.tsx around lines 20 to 119, add edge-case tests to cover null/undefined children, empty fragments, nested wrappers, and malformed options [error]: ============================================================================
  53. packages/testing/src/render.test.tsx [nitpick]: Line: 39 to 48
  54. packages/testing/src/render.test.tsx around lines 39 to 48, the test manually removes the appended container but that cleanup is skipped if an assertion throws; wrap the render and assertion in a try-finally (or add an afterEach that removes the container) so document.body.removeChild(container) is executed in the finally block (or moved to the afterEach) to guarantee cleanup even on test failure.: ============================================================================
  55. packages/testing/src/render.test.tsx [nitpick]: Line: 28 to 37
  56. packages/testing/src/render.test.tsx around lines 28 to 37, the test only asserts that RTL utilities are defined; update it to actually invoke a few utilities to verify they work: ============================================================================
  57. apps/routing/src/proxy.ts: Line: 84 to 93
  58. apps/routing/src/lib/testing-package.integration.test.tsx [nitpick]: Line: 61 to 83
  59. apps/routing/src/lib/testing-package.integration.test.tsx around lines 61-83, the factory tests only assert presence of properties; update assertions to validate types and formats (e.g., assert id is a non-empty string matching a UUID pattern, email is a string matching an email regex or contains: ============================================================================
  60. scripts/pr-coderabbit-comments.ts [nitpick]: Line: 70 to 85
  61. scripts/pr-coderabbit-comments.ts around lines 70 to 85, the explicit path-traversal check (lines 80–85) is redundant because the preceding regex validation (/^[0-9][A-Z]\.[0-9]+$/i) already restricts EPIC_NUMBER to a single digit, a letter, a literal dot, and digits so characters like "..", "/" and "\" cannot match; remove the unreachable block (lines 80–85) to eliminate dead code, or if you prefer defence-in-depth, keep the block but add a clear comment stating it is an intentional secondary safeguard and why it’s retained.: ============================================================================
  62. scripts/pr-coderabbit-comments.ts [nitpick]: Line: 346
  63. scripts/pr-coderabbit-comments.ts around line 346, the main function is declared async but contains no await; remove the unnecessary async keyword from the function declaration so it becomes a normal synchronous function, and verify any call sites that expected a Promise (e.g., main().catch(...)) are updated to call it directly or wrap it in Promise.resolve(main()) if they still require a Promise.: ============================================================================
  64. apps/routing/src/lib/testing-package.integration.test.tsx: Line: 100 to 125
  65. apps/routing/src/lib/testing-package.integration.test.tsx around lines 100 to 125, the current single test only exercises multiple server.use calls but doesn: ============================================================================
  66. scripts/pr-coderabbit-comments.ts [nitpick]: Line: 46 to 48
  67. scripts/pr-coderabbit-comments.ts around lines 46 to 48, OWNER and REPO are hardcoded which reduces reusability; change them to read from environment variables (e.g., process.env.OWNER / process.env.REPO) and/or accept CLI arguments (using process.argv or a small arg parser), provide sensible defaults or fail-fast with a clear error if neither env nor arg is provided, and update any places that import/use OWNER/REPO to use the new variables; also add brief README or usage comments showing the environment variable names or CLI flags. [error]: ============================================================================
  68. apps/docs/proxy.ts [nitpick]: Line: 36 to 44
  69. apps/docs/proxy.ts around lines 36 to 44, replace the browser-style atob() usage with the Node.js-idiomatic Buffer API [error]: ============================================================================
  70. scripts/pr-coderabbit-comments.ts: Line: 314 to 318
  71. scripts/pr-coderabbit-comments.ts around lines 314 to 318 the code unconditionally writes triple-backtick fences around prompt.content which will corrupt Markdown if the content itself contains ; update the logic to choose a safe fence length (e.g., use "" when prompt.content includes ": ============================================================================
  72. scripts/pr-coderabbit-comments.ts [nitpick]: Line: 130 to 142
  73. scripts/pr-coderabbit-comments.ts around lines 130 to 142, the pagination loop can run indefinitely if the API doesn [error]: ============================================================================
  74. scripts/pr-coderabbit-comments.ts [nitpick]: Line: 97 to 110
  75. scripts/pr-coderabbit-comments.ts around lines 97 to 110, replace the current execSync string-invocation with execFileSync to avoid shell interpolation [error]: ============================================================================
  76. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json: Line: 36 to 38
  77. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json around lines 36-38, remove volatile fields (e.g., updated_at, pushed_at, size, watchers_count, open_issues_count) from the primary snapshot and keep only stable fields (created_at, number, title, head.ref, base.ref, status summary); either move any removed volatile metrics into an optional "live" block that is excluded from docs or omit them entirely; apply the same change to the other ranges noted in the comment (lines 146-176, 273-275, 281-304).: ============================================================================
  78. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json: Line: 13 to 16
  79. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json around lines 13–16 there is an epic numbering mismatch: ============================================================================
  80. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json [nitpick]: Line: 2520 to 2554
  81. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json: Line: 14 to 34
  82. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json around lines 14 to 34, the PR stores the full GitHub user/app public-profile and permission blocks; replace the verbose user object with only {"login", "id", "html_url"} and replace any full GitHub app objects with only {"name", "slug", "html_url"}; also condense performed_via_github_app.permissions to a short summary string (e.g., "limited read/write scopes" or a comma-separated list of top-level scopes) instead of the full permission matrix; apply the same reductions to the duplicate block at lines ~424-437.: ============================================================================
  83. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json [nitpick]: Line: 1595 to 1665
  84. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json around lines 1595-1665 (and also apply to 1659-1726), the review suggestions recommending removal of explicit page.waitForLoadState conflict with our team E2E convention of keeping explicit waits for clarity; update the document to either remove or annotate the auto-generated suggestion so it does not instruct removing waitForLoadState in tests/e2e/example.spec.ts, and add a short editorial note stating the project prefers explicit waitForLoadState calls in smoke/e2e tests—ensure all occurrences of the suggestion are replaced/annotated and tests retain their explicit waits. [suggestion]: ============================================================================
  85. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json [nitpick]: Line: 1802 to 1864
  86. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json: Line: 1 to 2
  87. docs/3-epics/1A.4-documentation-foundation/epic-1A.4-pr-18.json: Line: 377 to 421
  88. packages/testing/src/mocks/server.ts [nitpick]: Line: 52 to 77
  89. packages/testing/src/mocks/server.ts around lines 52 to 77, the module-scoped boolean isSetup lacks documentation about its worker-scoped behavior under Vitest; add a concise JSDoc comment immediately above the let isSetup = false declaration explaining that the guard prevents duplicate MSW server setup within the same worker process and that each Vitest worker gets its own server instance (this is intended behavior).: ============================================================================
  90. apps/routing/src/components/Button.tsx [nitpick]: Line: 10 to 20
  91. apps/routing/src/components/Button.tsx around lines 10 to 20, remove the unnecessary JSX import and avoid the explicit JSX.Element return type; instead let TypeScript infer the return type (or use React.ReactElement) and refactor the component to use React.forwardRef so callers can access the native button DOM node. Update the props type to React.ButtonHTMLAttributes (or keep ButtonProps but import from React) and accept a second ref parameter in the forwardRef call, passing it to the ; ensure the default type prop remains and spread remaining props onto the button.: ============================================================================
  92. packages/testing/src/factories/organization.ts [nitpick]: Line: 156 to 164
  93. packages/testing/src/factories/organization.ts around lines 156 to 164, the current implementation lets overrides.ownerId replace the explicit ownerId parameter which can be surprising; either document this override behavior in the function JSDoc or change the creation call so the explicit ownerId always wins (spread overrides first, then set ownerId explicitly or pass ownerId after merging) and add a unit test asserting the precedence.: ============================================================================
  94. packages/testing/src/factories/user.ts [nitpick]: Line: 103 to 106
  95. packages/testing/src/factories/user.ts around lines 103 to 106, the defensive check forcing updatedAt >= createdAt currently runs unconditionally and can overwrite explicit test data; change the logic to only enforce the rule when at least one of the dates was not provided by the caller (i.e., detect whether createdAt and/or updatedAt were passed in the factory overrides) — if both were explicitly provided, skip the adjustment so tests can create intentionally invalid date states; otherwise, keep the existing guard to correct generated/defaulted dates.: ============================================================================
  96. tests/e2e/example.spec.ts [nitpick]: Line: 40 to 48
  97. packages/testing/src/factories/organization.ts [nitpick]: Line: 72 to 93
  98. packages/testing/src/factories/organization.ts around lines 72 to 93, the slug generation unnecessarily computes finalName from overrides.name instead of using the resolved value already present in base after the spread; simplify by using base (which already contains overrides) when deciding the slug — return the object using base and set slug to base.slug ?? generateSlug(base.name) (or assign slug on base before returning) so the logic is clearer and avoids referencing overrides directly.: Review completed ✔