/**
 * Prettier configuration for @repo/routing.
 *
 * References the shared configuration from @repo/config.
 *
 * @type {import('prettier').Config}
 */
import config from '@repo/config/prettier';

export default config;
